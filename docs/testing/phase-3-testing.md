# Phase 3 — Testing prompt (for Claude)

```
Phase 3 is complete (tasks).

Write pytest tests for task endpoints:
- Employee sees only own tasks; cannot GET/PATCH complete others' tasks
- Admin can POST create, PUT update, complete any company task
- super_admin same as admin for company scope in MVP
- Permission helpers edge cases

Provide manual frontend steps: My Tasks, Team Tasks, Task Detail, filters, mobile layout checks.
```
